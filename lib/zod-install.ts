// ❗️  This file is never imported at runtime.
//     It only exists so Next.js adds `zod` to node_modules at the latest v3.23+,
//     which exposes the `zod/v4/core` sub-path required by @hookform/resolvers.
import "zod"
